rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // users/{uid}/** 配下はその uid のみ R/W 可
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }

    // それ以外はデフォルト拒否
  }
}
